@{
    ViewBag.Title = "Saldo";
}

<partial name="_NavUser" />
<div class="container pb-5">
    <h1 class="h3">Saldo disponible</h1>
    <p class="text-secondary">Cuenta @ViewBag.NoCuenta</p>
    <p class="display-6">$@(((decimal)ViewBag.Saldo).ToString("N2"))</p>
</div>
